# Copy to .env.local for local development. Never commit .env.local.

# Backend API base URL. Server-only (no NEXT_PUBLIC_ prefix) - this
# value, and everything the backend itself keeps secret (SPORTMONKS_API_TOKEN,
# DATABASE_URL), never reaches client-side JavaScript.
API_BASE_URL=http://localhost:8000
